CREATE PROC usp_GetOlder (@id INT)
AS
BEGIN
	 BEGIN TRANSACTION

	UPDATE Minions
	   SET Age += 1
	 WHERE Id = @id

	 IF (@@ROWCOUNT <> 1)
	 BEGIN
		ROLLBACK
		RAISERROR('Invalid Id', 16 ,1)
		RETURN
	 END

	 COMMIT
END